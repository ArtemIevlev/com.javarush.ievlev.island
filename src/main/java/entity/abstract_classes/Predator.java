package entity.abstract_classes;

import entity.Tile;

public class Predator extends Animal{

    public Predator(int weight, int maxOnTile, Tile currentTile) {
        super(weight, maxOnTile, currentTile);
    }
}
